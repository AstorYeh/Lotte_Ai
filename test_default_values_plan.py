# -*- coding: utf-8 -*-
"""
方案 A: 默認值優化 - 簡化版
直接測試最有希望的默認值
"""

# 基於分析,我們認為最優默認值可能在 0.3-0.5 之間
# 
# 測試策略:
# 1. 先測試 0.5 (原始值)
# 2. 如果 0.5 最優,則完成
# 3. 如果不是,再測試 0.3, 0.35, 0.4, 0.45
#
# 每個值執行完整 305 期訓練,確保結果可靠

# 修改 enhanced_models.py 的步驟:
# 
# 1. 找到兩處 "scores[num] = 0.15" (XGBoost 和 Random Forest)
# 2. 改為 "scores[num] = 0.5" (或其他測試值)
# 3. 執行 train_full_baseline.py
# 4. 分析結果
# 5. 記錄並對比

print("""
方案 A: 默認值優化測試計劃

目標: 找出最優默認值,超越原始基準 20.65%

測試值:
1. 0.5  (原始值,最有希望)
2. 0.4  (如果 0.5 不是最優)
3. 0.3  (如果 0.4 不是最優)

執行步驟:
1. 手動修改 src/enhanced_models.py
   - 將兩處 "scores[num] = 0.15" 改為測試值
   - XGBoost: 第 63 行
   - Random Forest: 第 130 行

2. 執行完整訓練
   python train_full_baseline.py

3. 分析結果
   python analyze_profit_rate.py

4. 記錄結果並對比

5. 重複步驟 1-4,測試下一個值

預期結果:
- 最優默認值: 0.5 (可能)
- 2+ 命中率: 20.5-21.5%
- 超越原始基準 20.65%

建議:
- 優先測試 0.5,因為這是原始值
- 如果 0.5 達到 20.5%+,則成功
- 如果不是,再測試 0.4, 0.3
""")
